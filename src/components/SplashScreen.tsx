const SplashScreen = () => (
  <div className="flex items-center justify-center h-screen bg-background">
    <div className="text-5xl animate-pulse" style={{ animationDuration: '1s' }}>
      💝
    </div>
  </div>
);

export default SplashScreen;
