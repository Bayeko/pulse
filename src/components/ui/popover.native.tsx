import React from 'react';
import { View } from 'react-native';
export const Popover = (props: any) => <View {...props} />;
export const PopoverTrigger = (props: any) => <View {...props} />;
export const PopoverContent = (props: any) => <View {...props} />;
